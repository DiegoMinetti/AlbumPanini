# Base path for the build. On GitHub Pages set this to "/<repo-name>/".
# Locally leave as "/" (default).
VITE_BASE_PATH=/

# App version surfaced in the UI / backups. Usually injected by CI.
VITE_APP_VERSION=1.0.0
